# Документация требований

Дерево функциональных требований по фичам — что должно делать
приложение и как это устроено на клиенте/сервере. Формат и правила
именования — `STYLE_GUIDE.md` в этой же папке.

Отличие от остальных доков в `docs/`:
- `docs/ARCHITECTURE.md` — **почему** так устроено технически (паттерны,
  компромиссы, gotchas)
- `docs/DEPLOYMENT.md` — продовая инфраструктура
- `docs/requirements/` (этот файл) — **что именно** должна делать каждая
  фича, по шагам, достаточно подробно, чтобы можно было восстановить
  поведение приложения только по этим файлам

## Дерево фич

- [01. Tasks](./01.%20Tasks.md)
  - [01.01. Task CRUD-FRONTEND](./01.%20Tasks/01.01.%20Task%20CRUD-FRONTEND.md)
  - [01.01. Task CRUD-BACKEND](./01.%20Tasks/01.01.%20Task%20CRUD-BACKEND.md)
  - [01.02. Categories-FRONTEND](./01.%20Tasks/01.02.%20Categories-FRONTEND.md)
  - [01.02. Categories-BACKEND](./01.%20Tasks/01.02.%20Categories-BACKEND.md)
- [02. Goals](./02.%20Goals.md)
  - [02.01. Goal CRUD-FRONTEND](./02.%20Goals/02.01.%20Goal%20CRUD-FRONTEND.md)
  - [02.01. Goal CRUD-BACKEND](./02.%20Goals/02.01.%20Goal%20CRUD-BACKEND.md)
- [03. Habits](./03.%20Habits.md)
  - [03.01. Habit CRUD-FRONTEND](./03.%20Habits/03.01.%20Habit%20CRUD-FRONTEND.md)
  - [03.01. Habit CRUD-BACKEND](./03.%20Habits/03.01.%20Habit%20CRUD-BACKEND.md)
  - [03.02. Habit History-FRONTEND](./03.%20Habits/03.02.%20Habit%20History-FRONTEND.md)
    (без BACKEND — использует существующие методы Habit CRUD)
  - [03.03. Habit Groups-FRONTEND](./03.%20Habits/03.03.%20Habit%20Groups-FRONTEND.md)
  - [03.03. Habit Groups-BACKEND](./03.%20Habits/03.03.%20Habit%20Groups-BACKEND.md)
  - [03.04. Today Habit Groups-FRONTEND](./03.%20Habits/03.04.%20Today%20Habit%20Groups-FRONTEND.md)
    (без BACKEND — использует те же методы Habit/HabitGroup CRUD)
- [04. Calendar](./04.%20Calendar.md)
  - [04.01. Calendar Infrastructure-FRONTEND](./04.%20Calendar/04.01.%20Calendar%20Infrastructure-FRONTEND.md)
    (без BACKEND — использует обычный CRUD задач/целей/напоминаний/
    привычек, своих серверных методов нет)
  - [04.02. Month View-FRONTEND](./04.%20Calendar/04.02.%20Month%20View-FRONTEND.md)
  - [04.03. Agenda View-FRONTEND](./04.%20Calendar/04.03.%20Agenda%20View-FRONTEND.md)
  - [04.04. Day & Week Timeline-FRONTEND](./04.%20Calendar/04.04.%20Day%20%26%20Week%20Timeline-FRONTEND.md)
  - [04.05. Year View-FRONTEND](./04.%20Calendar/04.05.%20Year%20View-FRONTEND.md)
- [05. Today (Dashboard)](./05.%20Today.md)
  - [05.01. Today Dashboard-FRONTEND](./05.%20Today/05.01.%20Today%20Dashboard-FRONTEND.md)
    (без BACKEND — собирает уже существующие данные, не хранит своих)
- [06. Navigation](./06.%20Navigation.md)
  - [06.01. Sidebar Stats-FRONTEND](./06.%20Navigation/06.01.%20Sidebar%20Stats-FRONTEND.md)
    (без BACKEND — чисто клиентские вычисления над уже загруженными
    данными)
- [07. Localization](./07.%20Localization.md)
  - [07.01. Language Toggle-FRONTEND](./07.%20Localization/07.01.%20Language%20Toggle-FRONTEND.md)
    (без BACKEND — язык не привязан к аккаунту, авторизации нет)
- [08. Theme](./08.%20Theme.md)
  - [08.01. Theme Toggle-FRONTEND](./08.%20Theme/08.01.%20Theme%20Toggle-FRONTEND.md)
    (без BACKEND — тема не привязана к аккаунту, авторизации нет)
- [09. Authentication](./09.%20Authentication.md)
  - [09.01. Account Access-FRONTEND](./09.%20Authentication/09.01.%20Account%20Access-FRONTEND.md)
  - [09.01. Account Access-BACKEND](./09.%20Authentication/09.01.%20Account%20Access-BACKEND.md)
- [10. Feedback](./10.%20Feedback.md)
  - [10.01. Feedback Widget-FRONTEND](./10.%20Feedback/10.01.%20Feedback%20Widget-FRONTEND.md)
  - [10.01. Feedback Widget-BACKEND](./10.%20Feedback/10.01.%20Feedback%20Widget-BACKEND.md)
- [11. Onboarding](./11.%20Onboarding.md)
  - [11.01. Welcome Screens-FRONTEND](./11.%20Onboarding/11.01.%20Welcome%20Screens-FRONTEND.md)
    (без BACKEND — чисто клиентские экраны поверх уже описанного флоу
    Better Auth)
- [12. Settings](./12.%20Settings.md)
  - [12.01. Settings Panel-FRONTEND](./12.%20Settings/12.01.%20Settings%20Panel-FRONTEND.md)
    (без BACKEND — своего серверного метода нет, использует
    `POST /api/auth/change-password`, описанный в
    [09.01. Account Access-BACKEND](./09.%20Authentication/09.01.%20Account%20Access-BACKEND.md))
- [13. Reminders](./13.%20Reminders.md)
  - [13.01. Due Date Reminders-FRONTEND](./13.%20Reminders/13.01.%20Due%20Date%20Reminders-FRONTEND.md)
    (без BACKEND — читает уже загруженные `tasks`/`goals`, ничего не
    отправляет на сервер)
  - [13.02. Reminder Entity-FRONTEND](./13.%20Reminders/13.02.%20Reminder%20Entity-FRONTEND.md)
  - [13.02. Reminder Entity-BACKEND](./13.%20Reminders/13.02.%20Reminder%20Entity-BACKEND.md)
  - [13.03. Reminder Bell-FRONTEND](./13.%20Reminders/13.03.%20Reminder%20Bell-FRONTEND.md)
    (без BACKEND — читает уже загруженные `reminders`, ничего не
    отправляет на сервер сверх уже существующего CRUD)
  - [13.04. Critical Reminder Alerts-FRONTEND](./13.%20Reminders/13.04.%20Critical%20Reminder%20Alerts-FRONTEND.md)
    (без BACKEND — своей серверной логики нет, `'priority'` уже часть
    `13.02. Reminder Entity-BACKEND`)

Дерево полностью покрывает текущий функционал приложения (обновлено
2026-09-22 — сущность `Reminder` получила поле `'priority'`
(`'normal'`/`'critical'`, см. "13.02. Reminder Entity"): циклический
переключатель в форме, визуальный признак приоритета в списках и фон в
сетке календаря (форма признака менялась позже, см. запись ниже,
2026-09-22), кастомный `TimePicker` заменил нативный `<input
type="time">`. На Today
карточка-переключатель "Прогресс целей ↔ Напоминания" обзавелась
бейджем/тултипом с числом предстоящих напоминаний и автоматически
открывается на "Напоминаниях", если они есть. Тот же бейдж и попап
ближайших напоминаний теперь доступны глобально на каждой странице через
новый "13.03. Reminder Bell". Критичные напоминания дополнительно
получили собственный модальный поп-ап по мере приближения срока —
"13.04. Critical Reminder Alerts").
Дальше — поддерживать в актуальном состоянии по `<WhenToUpdate>` из
`STYLE_GUIDE.md`: новая функциональность → новый `NN.MM.` файл,
изменение поведения → правка существующего файла, а не создание нового
рядом.

Обновлено 2026-09-22 (три независимые правки, без новых файлов в дереве):
клик по строке цели на Today теперь раскрывает эту цель на странице
Goals и переставляет её первой в списке (было — сразу открывало форму
редактирования), см. "02. Goals"; цветные кружки-индикаторы приоритета у
задач и напоминаний заменены иконками (`PriorityDot`/
`ReminderPriorityIcon`, форма намекает на уровень, не только цвет),
приоритет напоминания теперь виден для обоих уровней (`normal`/
`critical`), а не только критичного, и попутно закрыт пробел — карточка
сегодняшних задач на Today вообще не показывала приоритет, см. "01.
Tasks" и "13. Reminders"; график под сеткой истории привычек выровнен
пиксель-в-пиксель с колонками дат сетки и получил переключатель из 4
стилей визуализации (area/bar/step/ring), см. "03. Habits".

Обновлено 2026-09-23 (три независимые правки, без новых файлов в дереве):
правило видимости бессрочной (без `'dueDate'`) задачи на Today
пересмотрено ещё раз — невыполненная видна каждый день, пока не будет
выполнена (не только в день создания, как было с 2026-09-21), выполненная
пропадает на следующий день после `'completedAt'`, закрыто unit-тестом,
см. "05. Today"; строка задачи (Tasks/Today/панель дня Calendar)
выровнена по верхнему краю вместо центра — длинные названия в 2+ строки
больше не сдвигают чекбокс/иконку приоритета/бейдж категории вниз
относительно однострочных задач рядом, см. "01. Tasks"; стиль "кольцо"
переключателя графика истории привычек убран по обратной связи
("непонятно, где круг и процентовка") — остались area/bar/step, `Ring`-
компонент и всё, что было нужно только ему, удалены, а не скрыты, см.
"03. Habits".

Обновлено 2026-09-23 (второй заход в тот же день, пять точечных правок
в "Напоминаниях" + одна в "Обратной связи"): дата напоминания теперь
всегда редактируема при правке (раньше `lockedDate`, нужный только для
быстрого добавления из панели дня, случайно делал дату НАВСЕГДА
нередактируемой); дата видна в списках на Today и в попапе глобального
колокольчика (раньше только время); бейдж-счётчик остался только на
глобальном колокольчике, убран с дублирующей локальной кнопки на
карточке Today (заодно снял наложение тултипа на кнопку "Добавить
задачу"); иконка приоритета в строке напоминания — в колонке
фиксированной ширины, текст больше не гуляет по горизонтали в
зависимости от того, какая иконка отрисовалась — см. "13. Reminders".
Письмо с обратной связью показывает имя/email отправителя вместо
служебного `userId` (лукап по уже существующему аккаунту, без нового
поля в форме) — см. "10. Feedback".

Обновлено 2026-09-23 (третий заход в тот же день, четыре точечных правки,
без новых файлов в дереве): глобальный колокольчик (`13.03. Reminder
Bell`) скрыт на `/today` — там теперь только локальная кнопка на карточке
`GoalReminderSwapCard`, и бейдж-счётчик/`Tooltip`, убранные с неё в
предыдущем (втором) заходе того же дня как дублирующие, вернулись на неё
же, раз она снова единственный источник этой информации на своей
странице (см. "13. Reminders"); иконка приоритета у задач (Tasks, Today)
наконец выстроена в одну колонку — исправлено через фиксированную
ширину `w-20` бейджа категории (`TaskRow.tsx`, `TodayTaskRow.tsx`),
переопределяющую его дефолтный `w-fit`, причина отдельная от выравнивания
`items-start`, закрытого тем же днём раньше (см. "01. Tasks"); строка
напоминания (`ReminderRow`, попап `ReminderBell`) переверстана на
двухколоночный `grid` — заголовок больше не обрезается серединой слова
теперь, когда в той же строке есть дата (см. "13. Reminders"); счётчик
"выполнено" в заголовке страницы Tasks считается только за сегодня
(`isCompletedToday`), а не за всё время — согласован со списком задач
под заголовком, который уже скрывал старые выполненные задачи (см. "01.
Tasks").

Обновлено 2026-09-23 (четвёртый заход в тот же день, точечный фикс
одного компонента): возврат бейджа/tooltip на кнопку `GoalReminderSwapCard`
(предыдущая запись выше) принёс два мелких бага — оба показывались
независимо от того, какая иконка на кнопке (в том числе бессмысленно на
"Target", когда напоминания и так уже видны), и tooltip снова упирался в
кнопку "Add task" сверху, только с новой стороны. Оба закрыты — см. "13.
Reminders".

Обновлено 2026-09-23 (пятый заход в тот же день, новая крупная фича,
новый файл "03.03. Habit Groups"): привычки на странице `/habits`
сгруппированы в именованные сворачиваемые блоки (своя иконка, свои
сетка/график/период на каждый блок) — новая сущность `HabitGroup`, с
реальным foreign key от `Habit.groupId` (в отличие от `Task.category`,
который остаётся строкой), полный CRUD-роутер `/api/habit-groups`, и
неудаляемый/непереименовываемый автосидируемый блок "Общие"
(`isGeneral: true`) для привычек вне пользовательских блоков; блоки и
привычки внутри/между блоками перетаскиваются. Today не затронут —
виджет привычек там остался плоским списком. См. "03. Habits", "03.02.
Habit History-FRONTEND" (сетка/график теперь в масштабе одного блока) и
новые "03.03. Habit Groups-FRONTEND"/"03.03. Habit Groups-BACKEND". По
ходу QA исправлены два переиспользуемых бага общего назначения (не
специфичных для привычек): `useDropTarget` дублировал `onDrop` при
вложенных drop-зонах одного типа (`monitor.didDrop()`); `reorder()` в
`usePersistedCollection` раньше отбрасывал элементы вне переданного
подмножества коллекции — теперь их сохраняет, что нужно для
переупорядочивания привычек в рамках одного блока.

Обновлено 2026-09-24 (три связанные правки той же фичи, один новый файл
"03.04. Today Habit Groups"): группа "Общие" (`isGeneral: true`) стала
полноценно редактируемой — `PATCH` над ней теперь разрешён (название,
иконка, новое поле `HabitGroup.color`), запрещено по-прежнему только
удаление; группировка привычек по блокам, раньше существовавшая только
на `/habits`, распространена и на главный экран (Today) — виджет
привычек там теперь тоже список карточек-блоков (по одной на блок, у
которого есть хоть одна привычка на сегодня), с цветной "пилюлей"-
заголовком (`HabitGroup.color`) вместо обычного текста, как на `/habits`;
при этом порядок привычек **внутри** блока стал независимым по экранам —
новое поле `Habit.todayOrder` и роут `PATCH /api/habits/reorder-today`
(точная копия `/reorder`, но пишет другую колонку) обслуживают Today
отдельно от `Habit.order`/`/reorder`, которые остались за `/habits`;
принадлежность привычки к блоку (`'groupId'`) и порядок самих блоков
относительно друг друга — по-прежнему общие для обоих экранов.
`usePersistedCollection.reorder()` получил необязательный второй
параметр (какую сетевую функцию вызывать) — так вся защита от гонок при
перестановке переиспользуется для новой независимой оси без дублирования
кода. См. "03. Habits", новые "03.03. Habit Groups-BACKEND" (редактируемая
"Общие", `color`), "03.03. Habit Groups-FRONTEND" (условная подмена
локализованного названия — только пока оно ещё равно заглушке — и
цветовой пикер блока) и новый "03.04. Today Habit Groups-FRONTEND".

Обновлено 2026-09-24 (крупная фича, "04. Calendar" полностью
переработана в пять видов, один бывший файл разделён на пять новых):
календарь в стиле TickTick получил переключатель видов — Повестка дня
(плоский список на 30 дней вперёд), День и Неделя (часовая шкала +
строка "весь день", перетаскивание задачи по времени), Месяц (прежний
единственный вид, без изменения поведения), Год (12 мини-месяцев,
клик по дню переключает на "День"). Задача получила новое поле
`'time'` (`"HH:mm"`, аддитивная nullable-миграция, тот же паттерн, что у
`Reminder.time`) и общий с напоминанием `TimePicker` (переехал из
`features/reminder-form/ui/components` в `shared/ui`, ничего
reminder-специфичного в нём не было). Цели и привычки, раньше видимые
только на своих страницах, теперь тоже показываются на календаре
(`isGoalDueOnDay`, `selectHabitsOnDay` — новые селекторы; перенос цели на
другой день перетаскиванием — новое действие `moveGoalDeadline`).
`buildMonthGrid` переехал из `widgets/calendar-grid/lib` в `shared/lib`,
потому что виджет "Год" не может импортировать из виджета-соседа
(`fsd/forbidden-imports`). Старый единственный файл "04.01.
Calendar-FRONTEND" удалён, вместо него — новые "04.01. Calendar
Infrastructure-FRONTEND" (общее состояние/навигация/URL, разделяемые
всеми видами), "04.02. Month View-FRONTEND" (прежнее поведение плюс цели/
привычки в панели дня), "04.03. Agenda View-FRONTEND", "04.04. Day & Week
Timeline-FRONTEND", "04.05. Year View-FRONTEND". Заодно правки в "01.
Tasks" (`'time'` в `TaskForm`) и "02. Goals" (`'defaultTargetDate'` в
`GoalForm` для добавления цели с конкретным сроком из календаря) —
ссылки на старый файл "04.01. Calendar-FRONTEND" в "01. Tasks" и "13.
Reminders" обновлены на новые файлы-преемники.

Обновлено 2026-09-24 (третий раунд правок календаря в тот же день, без
новых файлов в дереве): пересекающиеся по времени задача и напоминание на
часовой шкале Дня/Недели теперь раскладываются в отдельные "дорожки"
рядом (`computeLanes`, классический алгоритм "meeting rooms"), а не
наслаиваются друг на друга — закрывает найденный баг; стек отдельных
кнопок-тумблеров срока/времени в формах задачи, напоминания и цели
заменён одним полем `DateTimeField`, старый компонент `DatePicker`
удалён; "Неделя" (в предыдущем раунде того же дня сознательно не
тронутая) дотянута до полного механического паритета с "Днём" — та же
статическая боковая панель с формами убрана, вместо неё тот же попап
`CalendarItemPopover`, то же создание/резайз по часовой сетке, и у Недели
тоже не осталось никакого CRUD цели. См. "04. Calendar" →
"04.04. Day & Week Timeline-FRONTEND" (переписан почти полностью) и
точечные правки в "01. Tasks", "02. Goals", "07. Localization", "13.
Reminders" (замена ссылок на `DatePicker`/`date-picker.tsx` на
`DateTimeField`/`date-time-field.tsx`).

Обновлено 2026-09-25 (точечная правка календаря, без новых файлов в
дереве): пассивные маркеры цели/привычки убраны из двух оставшихся мест
по прямой повторной обратной связи, что они непонятны и плохо читаются
без подписи — строка "весь день" Дня/Недели (`AllDayRow`) лишилась чипа
цели (`AllDayGoalChip`) и счётчика привычек (иконка `Repeat` + число),
ячейка дня в сетке "Месяца" (`CalendarDayCell`) лишилась нижнего ряда из
иконок `Target`/`Repeat`. Чисто отображение — перетаскивание цели на
ячейку "Месяца" для смены `'targetDate'` и панель дня "Месяца" (полный
список целей/привычек по имени) не затронуты, Agenda и Year не затронуты
вовсе (там цели/привычки и так были текстом, не иконками). См.
"04. Calendar" → "04.02. Month View-FRONTEND" и "04.04. Day & Week
Timeline-FRONTEND".

Обновлено 2026-09-25 (второй, более поздний заход в тот же день, две
независимые правки, без новых файлов в дереве): строка "весь день" Дня/
Недели (`AllDayRow`, выше в этом же списке лишившаяся только маркеров
цели/привычки) убрана **целиком** — по дальнейшей прямой обратной связи,
что сама ячейка с кнопками "+" читается как непонятный промежуточный
слой между датой и календарём. `AllDayRow.tsx` удалён из репозитория,
`CalendarDayView`/`CalendarWeekView` теперь ведут от заголовка сразу к
часовой сетке; недатированный (без `'time'`) элемент теперь нигде не
отображается в Дне/Неделе конкретно (по-прежнему виден в Месяце, Повестке
дня, на `/tasks`/`/reminders`), `onMoveTaskToDay`/`moveTaskToDay` стал
мёртвым пропом для этих двух видов (Месяц им по-прежнему пользуется),
ключ `calendar.addUntimed` удалён. Отдельно, не связанным по причине
фиксом — граница выбранного сегмента `ToggleGroup`-тумблера
"Задача"/"Напоминание" в попапе создания стала видимой
(`data-[variant=outline]:border-border` вместо почти прозрачной
`--input`-границы в `shared/ui/toggle-group.tsx`) — раньше заливка
выбранного варианта визуально "перетекала" в соседний без разделения;
общий компонент, тот же фикс затронул и переключатель сетка/список
привычек на `/habits`, без регрессии там. См. "04. Calendar" → "04.04.
Day & Week Timeline-FRONTEND" (переписан почти полностью) и точечная
правка в "03. Habits" → "03.01. Habit CRUD-FRONTEND".
