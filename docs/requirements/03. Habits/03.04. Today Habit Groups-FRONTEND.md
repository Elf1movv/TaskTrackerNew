# 03.04. Today Habit Groups [Frontend]

# Описание

С 2026-09-24 виджет "Привычки" на главном экране (Today) тоже показывает
привычки сгруппированными в блоки — до этого он был плоским списком
(`HabitCard`/`HabitListRow` без обёртки по блокам). Сущность блока
(`HabitGroup`), её CRUD и общий с `/habits` список/порядок блоков
описаны в `03.03. Habit Groups-FRONTEND.md` — этот файл описывает только
то, что отличается на Today: карточка блока (`TodayHabitGroupCard`),
фильтр "только блоки с привычками на сегодня" и независимый от `/habits`
порядок привычек внутри блока (`'todayOrder'`).

Своей серверной логики нет — использует те же методы `Habit`/
`HabitGroup` CRUD, что и `/habits` (см. `03.01. Habit CRUD-BACKEND.md`,
`03.03. Habit Groups-BACKEND.md`), включая отдельный роут `PATCH
/api/habits/reorder-today`.

# Где находится (файлы)

- `src/widgets/habit-tracker-grid/ui/HabitTrackerGrid.tsx` — сам виджет:
  список карточек блоков, кнопка "+ Добавить блок", переключатель вида
  (сетка/список)
- `src/widgets/habit-tracker-grid/ui/TodayHabitGroupCard.tsx` — одна
  карточка блока: цветная "пилюля"-заголовок, свой аккордеон, своя
  кнопка "+ Добавить привычку", свой drag-and-drop привычек
- `src/widgets/habit-tracker-grid/ui/components/HabitGridItem.tsx` —
  одна привычка внутри карточки блока (источник и цель перетаскивания
  типа `"habit-move-today"`)
- `src/widgets/habit-tracker-grid/lib/habitViewMode.ts` — переключатель
  вида (сетка/список), общий на весь виджет, персистится в
  `localStorage` (см. `03.01. Habit CRUD-FRONTEND.md`, п. 10)
- `src/entities/habit/model/HabitProvider.tsx` — `reorderHabitsToday`,
  `moveHabitToGroupToday`
- `src/entities/habit/api/habitRepository.ts` — `reorderToday` (шлёт
  `PATCH /api/habits/reorder-today`)
- `src/entities/habit-group/model/HabitGroupProvider.tsx` —
  `reorderHabitGroups` (общий с `/habits`, см. `03.03. Habit
  Groups-FRONTEND.md`)
- `src/entities/habit-group/lib/habitGroupDisplay.ts` —
  `getHabitGroupIcon()`/`getHabitGroupTitle()`, те же, что на `/habits`
  (см. `03.03. Habit Groups-FRONTEND.md`)
- `src/shared/lib/dnd/useDragReorder.ts`, `useDropTarget.ts` — общий
  drag-and-drop механизм, здесь с типами `"habit-group"` (блоки, общий с
  `/habits`) и `"habit-move-today"` (привычки, отдельный от `/habits`'
  `"habit-move"`)
- `src/pages/today/connectors/TodayProvider.tsx` — `habits`
  (`selectTodayHabits`, фильтр по `'activeDays'`, см. `05.01. Today
  Dashboard-FRONTEND.md`) — привычки, уже отфильтрованные по
  сегодняшнему дню недели, передаются в `HabitTrackerGrid` дальше по
  дереву компонентов

# Функциональные требования

## Какие блоки показываются

1. `HabitTrackerGrid` рендерит по одной `TodayHabitGroupCard` на каждый
   `HabitGroup`, у которого есть хотя бы одна привычка среди уже
   отфильтрованных по сегодняшнему дню недели (`groupsWithHabitsToday =
   habitGroups.filter(g => habits.some(h => h.groupId === g.id))`,
   `HabitTrackerGrid.tsx:26`) — эта фильтрация не про расписание внутри
   блока, а про то, показывать ли карточку блока вообще
2. Блок без единой привычки на сегодня **не показывается на Today
   вовсе** — ни пустой карточкой, ни как-то иначе. Он при этом остаётся
   полностью видимым и управляемым на `/habits` (там показываются все
   привычки блока, без фильтра по дню недели, см. `03.02. Habit
   History-FRONTEND.md`)
3. Пустое состояние всего виджета — "Пока нет привычек"
   (`habits.noHabitsYet`), показывается, только если **ни один** блок
   не прошёл фильтр из п. 1 и форма добавления блока не открыта
   (`HabitTrackerGrid.tsx:78-82`) — тот же ключ перевода, что использует
   пустое состояние отдельного блока на `/habits` (см. `03.02. Habit
   History-FRONTEND.md`), но здесь про весь виджет целиком

## Карточка блока

4. Заголовок карточки (`TodayHabitGroupCard.tsx:97-111`) — не
   левовыровненный текст, как на `/habits`, а центрированная овальная
   "пилюля": `backgroundColor: group.color`, внутри — иконка блока
   (`getHabitGroupIcon`) и локализованное название (`getHabitGroupTitle`),
   белый текст. Область вокруг "пилюли" (весь `div` заголовка) — ручка
   перетаскивания блока (`cursor-grab`) и одновременно drop-зона для
   привычки, перетащенной из другого блока (см. п. 8)
5. Кнопки редактирования/удаления блока — в правом верхнем углу
   заголовка, абсолютно спозиционированы (`absolute right-4 top-4`), не
   в общей строке с "пилюлей": `AccordionTrigger` без соседа по
   flex-строке растягивается на всю ширину и получает реальное
   свободное место для центрирования "пилюли" через `justify-center`
   (см. `LEARNING.md`, 2026-09-24, про `AccordionPrimitive.Header`,
   `fit-content`-ширину триггера и опасность оставленных `mx-auto`)
6. Каждая карточка — свой независимый `Accordion` (`isOpen`, локальный
   `useState`, по умолчанию `true`) — сворачивание одной карточки не
   влияет на другие, тот же паттерн, что у блоков на `/habits`
   (`HabitGroupAccordionItem`, см. `03.03. Habit Groups-FRONTEND.md`,
   п. 10)
7. Добавление привычки — только через "+" внутри конкретной карточки
   блока (`habits.addHabit`, `HabitForm` с `'lockedGroupId'` = этого
   блока) — у виджета Today больше нет общей кнопки "+ Добавить
   привычку" на уровне всего виджета (была до 2026-09-24, когда виджет
   был плоским списком)
8. Пустое состояние карточки — "Пока нет привычек" (`habits.noHabitsYet`),
   на уровне блока, если в блоке нет ни одной привычки на сегодня и
   форма добавления не открыта — тот же паттерн, что на `/habits` (см.
   `03.02. Habit History-FRONTEND.md`, п. 4)

## Независимый порядок привычек внутри блока (`'todayOrder'`)

9. Привычка внутри одного блока переставляется перетаскиванием карточки/
   строки на другую в том же блоке — `reorderHabitsToday(groupId,
   draggedId, targetId)` (`HabitProvider.tsx`), пишет `'todayOrder'`, а
   не `'order'`. Это тот же общий `useDragReorder`/`usePersistedCollection
   .reorder()` механизм, что и везде в приложении, но использует
   отдельный тип перетаскивания `"habit-move-today"` (не пересекается с
   `/habits`' `"habit-move"` — технически не может, так как это разные
   страницы, но типы разведены явно, а не полагаются на это)
10. Привычку можно перетащить в **другой** блок на Today — на заголовок
    другого блока или на строку конкретной привычки в нём —
    `moveHabitToGroupToday(habitId, targetGroupId)`: сначала дожидается
    серверного подтверждения смены `'groupId'` (`update()`), затем одним
    отдельным вызовом переставляет привычку в конец списка привычек
    целевого блока по оси `'todayOrder'` (`reorder(...,
    habitRepository.reorderToday)`) — тот же порядок действий и та же
    причина (избежать ложного конфликта "changed elsewhere"), что и у
    `moveHabitToGroup` на `/habits`, см. `03.03. Habit
    Groups-FRONTEND.md`, п. 22
11. Принадлежность привычки к блоку (`'groupId'`) — общая с `/habits`:
    `moveHabitToGroupToday` и `moveHabitToGroup` меняют один и тот же
    `'groupId'`. Независим только порядок **внутри** списка — привычка,
    переставленная внутри блока на Today, не меняет свою позицию среди
    привычек того же блока на `/habits`, и наоборот
12. `usePersistedCollection.reorder()` принимает необязательный второй
    аргумент — какую сетевую функцию вызывать вместо `repository.reorder`
    по умолчанию (`src/shared/lib/storage/usePersistedCollection.ts`).
    `reorderHabitsToday`/`moveHabitToGroupToday` передают
    `habitRepository.reorderToday` этим аргументом — так вся защита от
    гонок (очередь запросов, оптимистичное обновление, "посев" свежего
    `updatedAt` в `pendingUpdates` после ответа сервера, см.
    `LEARNING.md`, 2026-09-20/23) достаётся независимой оси `'todayOrder'`
    бесплатно, без дублирования кода `usePersistedCollection`
13. Новая привычка (`addHabit`) получает от сервера оба поля —
    `'order'` и `'todayOrder'` — сразу равными "последняя позиция в
    группе" на обоих экранах одновременно (см. `03.01. Habit
    CRUD-BACKEND.md`, `POST /api/habits`)

# Известные ограничения

1. Как и на `/habits`, перетаскивание привычки на конкретную строку в
   другом блоке не ставит её на позицию этой строки — она всегда
   добавляется в конец целевого блока по оси `'todayOrder'`
