# Конспект обучения — TaskTrackerNew

## Термины

- **компонент (component)** — переиспользуемый кусок интерфейса, оформленный
  как функция, которая возвращает разметку (JSX). Например, `TodayPage` и
  `AddTaskForm` — оба компоненты. (объяснено: 2026-08-13, при добавлении
  кнопки "Add task" на Today)
- **состояние (state)** — данные, которые компонент "запоминает" между
  перерисовками экрана и которые могут меняться со временем (например,
  "форма сейчас открыта или закрыта"). Создаётся через хук `useState`.
  (объяснено: 2026-08-13)
- **хук (hook)** — функция React, имя которой начинается с `use`
  (`useState`, `useTasks` и т.д.), и которая даёт компоненту дополнительные
  возможности — например, "память" между перерисовками. (объяснено:
  2026-08-13)
- **обработчик события (event handler)** — функция, которая выполняется в
  ответ на действие пользователя, например `onClick` — что делать по клику
  на кнопку. (объяснено: 2026-08-13)
- **условный рендеринг (conditional rendering)** — показ разной разметки в
  зависимости от состояния, например `{isAdding && <AddTaskForm />}` —
  показать форму, только если `isAdding` истинно. (объяснено: 2026-08-13)
- **импорт (import)** — способ подключить в файл код, который определён в
  другом файле или библиотеке, например `import { useState } from "react"`.
  (объяснено: 2026-08-13)
- **JavaScript / TypeScript / React / JSX-TSX — кто есть кто**: JavaScript
  (JS) — сам язык программирования; TypeScript (TS) — JS плюс типы (файлы
  `.ts`/`.tsx` в проекте); React — библиотека для интерфейсов, написанная на
  JS/TS (даёт нам компоненты, хуки, state, props); JSX/TSX — синтаксис,
  который позволяет писать HTML-подобную разметку внутри JS/TS-файла (TSX —
  то же самое, но в TypeScript-файле). (объяснено: 2026-08-13)
- **клиент / сервер** — клиент (браузер, наш React-фронтенд) и сервер (наша
  Express-программа) — два отдельных процесса, которые общаются по сети,
  даже если оба на одном компьютере. (объяснено: 2026-09-07, при разборе
  backend)
- **порт** — номер "двери", через которую конкретная программа принимает
  соединения на компьютере (5173 — фронтенд, 3001 — сервер, 5432 —
  PostgreSQL). (объяснено: 2026-09-07)
- **HTTP-запрос / метод** — сообщение от клиента к серверу по сети; `GET` —
  "дай мне данные", `PUT` — "замени данные на присланные". (объяснено:
  2026-09-07)
- **эндпоинт / роут (route)** — конкретный "адрес" на сервере, к которому
  можно обратиться, например `/api/tasks`. (объяснено: 2026-09-07)
- **JSON** — текстовый формат, в котором клиент и сервер пересылают друг
  другу данные. (объяснено: 2026-09-07)
- **ORM (Object-Relational Mapping)** — программа-переводчик (у нас —
  Prisma), которая превращает вызовы вроде `db.task.findMany()` в
  настоящий SQL-запрос к базе, чтобы не писать SQL руками. (объяснено:
  2026-09-07)
- **SQL** — язык запросов к реляционным базам данных (таким как
  PostgreSQL). (объяснено: 2026-09-07)
- **миграция (migration)** — файл с изменением структуры базы (например,
  "добавь таблицу Task с такими-то колонками"), который Prisma создаёт и
  применяет командой `prisma migrate dev` — как git-коммит, но для
  структуры базы данных. (объяснено: 2026-09-07)
- **SSH (Secure Shell)** — протокол для безопасного удалённого управления
  компьютером через командную строку; так мы будем заходить на VPS.
  (объяснено: 2026-09-15)
- **SSH-ключ (приватный / публичный)** — пара математически связанных
  файлов вместо пароля для входа на сервер: приватный (`id_ed25519`)
  никогда никуда не отправляется, живёт только на своём компьютере;
  публичный (`id_ed25519.pub`) можно свободно давать серверу — сам по себе
  для взлома бесполезен. Сервер проверяет, что у входящего есть подходящий
  приватный ключ, без передачи самого ключа по сети — в отличие от пароля,
  это невозможно подобрать перебором. (объяснено: 2026-09-15)
- **WHOIS** — публичная база данных о владельцах доменов (имя, адрес,
  email, телефон) — обязательная часть системы доменных имён, доступна
  всем. **WHOIS-приватность** — услуга регистратора, которая подставляет
  в эту базу свои контакты вместо настоящих владельца домена, чтобы
  личные данные не были видны публично. (объяснено: 2026-09-15)
- **Docker / контейнер / образ** — Docker упаковывает приложение вместе
  со всем нужным окружением (версия Node и т.п.) в один переносимый
  файл. **Образ** — "рецепт", что должно быть внутри; **контейнер** —
  уже запущенный экземпляр этого образа. Решает проблему "переезд на
  новый сервер = поставь Docker и запусти образ" вместо повторения всей
  ручной установки по `docs/DEPLOYMENT.md`. (объяснено: 2026-09-16)

## Решения по проекту

- **Кнопка добавления задачи на Today переиспользует `features/add-task`,
  а не создаёт новую форму** — форма уже существует и полностью работает,
  дублировать её нет смысла; ровно для такого переиспользования в FSD и
  существует слой `features`. (2026-08-13)
- **Бэкенд — Node.js + TypeScript** (Express + Prisma + PostgreSQL) — один
  язык на весь стек, типы (`Task`, `Goal`, `Habit`) можно переиспользовать
  между фронтендом и бэкендом без дублирования. (2026-09-07)
- **PUT заменяет всю коллекцию целиком**, а не отдельные ручки на
  create/update/delete — фронтенд и так уже был устроен вокруг
  `Repository<T>` (`list()`/`save(items)` — "отдай весь список"/"сохрани
  весь список"), поэтому свап на бэкенд не потребовал менять ни одного
  файла кроме самого `taskRepository.ts`/`goalRepository.ts`/
  `habitRepository.ts`. Минус: два устройства одновременно могут
  затереть правки друг друга — не страшно, пока нет синхронизации между
  устройствами; станет важно, когда до неё дойдём. **Заменено
  2026-09-16 на гранулярный API — см. ниже**, ровно по этой причине.
  (2026-09-07)
- **`Goal.milestones` хранится JSON-колонкой**, а не отдельной таблицей —
  чтобы не усложнять сверх уровня, который уже был у `Task`; можно
  нормализовать в отдельную таблицу позже, если понадобится. (2026-09-07)
- **`userId` — есть в схеме, но нигде не используется** — специально
  оставлено пустым (`nullable`) полем на будущее: когда будем делать
  логин, не придётся болезненно мигрировать уже существующие строки.
  (2026-09-07)
- **PostgreSQL поставили нативно через Homebrew, а не Docker** — Docker не
  был установлен, а Homebrew уже был; одной новой концепцией (контейнеры)
  меньше на этом этапе обучения. (2026-09-07)
- **Версии пакетов — "текущие стабильные", а не "самые новые из
  возможных"** — например, TypeScript 6.0.3, а не 7.0 (линтер проекта пока
  не поддерживает TS 7), Prisma 6.19.3, а не 7.x/8.0-rc (там нашлась
  уязвимость через неиспользуемый MySQL-драйвер, да и 8.0 — вообще ещё не
  стабильный релиз). Самое новое число — не всегда правильный выбор.
  (2026-09-07)
- **Свой VPS вместо управляемых платформ (Vercel/Railway)** — осознанный
  выбор: "работает на наших мощностях, без посредников". VPS — HipHosting,
  nginx (не Caddy — более стандартный и документированный инструмент)
  как reverse proxy, certbot для бесплатного HTTPS, GitHub Actions для
  автодеплоя при пуше в `main`. (2026-09-15 — 2026-09-16)
- **Гранулярный REST API вместо whole-collection PUT** —
  `POST`/`PATCH /:id`/`DELETE /:id`/`PATCH /reorder` на каждую сущность,
  плюс `'updatedAt'` в ответе для обнаружения конфликтов (если кто-то
  другой уже изменил ту же запись — `409` с актуальной версией, а не
  тихая перезапись). Понадобилось ровно тогда, когда предполагалось
  ("станет важно, когда до неё дойдём" — см. запись про PUT выше):
  начали активно пользоваться приложением с нескольких устройств.
  (2026-09-16)
- **Приложение и PostgreSQL — в Docker-контейнерах**, не установлены
  напрямую на VPS — переезд на новый сервер сводится к "поставь Docker,
  запусти `docker compose up`" вместо повторения всей ручной установки.
  Образ приложения собирается в GitHub Actions и хранится в GitHub
  Container Registry; Postgres — официальный образ с Docker Hub.
  Контейнеризация приложения и базы сделаны отдельными заходами
  (2026-09-16 и 2026-09-17) — осознанно не одновременно с рефакторингом
  API, чтобы при поломке было понятно, что именно её вызвало. Перенос
  базы включал реальную миграцию продовых данных (репетиция на копии,
  потом настоящее переключение с построчной сверкой количества записей).
  (2026-09-16 — 2026-09-17)
- **`Task.dueDate` — настоящая дата (`@db.Date`) в базе**, а не текстовая
  строка — нужно для нормальных SQL-запросов по диапазону дат в будущем
  (сейчас Calendar всё ещё фильтрует на клиенте, но фундамент готов).
  Миграция написана вручную (`ALTER COLUMN ... USING "dueDate"::date`),
  а не дефолтным способом Prisma (drop+recreate стёр бы существующие
  значения). На проводе для фронтенда формат не изменился — как был
  `"YYYY-MM-DD"` строкой, так и остался. (2026-09-17)
- **Тесты — и на бэкенде, и на фронтенде** — `vitest`+`supertest` для
  API (гоняются на отдельной тестовой базе, не на реальной), `vitest`+
  `@testing-library/react`+`TestBackend` от react-dnd для drag-and-drop
  хуков (того самого места, что три раза ломалось системно из-за
  нестабильных callback ref — теперь есть регресс-тест, который реально
  проверен на то, что ловит именно эту поломку). (2026-09-17)
- **Структурированная документация требований** (`docs/requirements/`) —
  по образцу корпоративного Confluence-стиля (FRONTEND/BACKEND-документы
  на каждую фичу, нумерация, обязательные секции) — чтобы можно было
  восстановить поведение приложения по одним только этим файлам, без
  необходимости перечитывать код или историю чата. (2026-09-15 — начато,
  2026-09-17 — дерево дописано полностью по всем фичам)
- **`Task.completedAt` — отдельное поле, а не переиспользование
  `updatedAt`** — нужно было понять, к какому дню отнести выполненную
  задачу (чтобы вкладка Tasks "All" не копила выполненные задачи вечно, а
  показывала только сегодняшние). `updatedAt` для этого не годится: он
  меняется при любом редактировании, а не только при отметке "выполнено"
  — переименование задачи неделю спустя после выполнения ложно "оживило"
  бы её в сегодняшнем дне. `completedAt` проставляется на клиенте ровно
  в момент переключения `completed`, сбрасывается в `null` при отмене.
  (2026-09-17)
- **Категории задач — отдельная сущность `Category` в базе, но БЕЗ
  внешнего ключа от `Task.category`** — раньше список категорий был
  просто зашитым массивом (`TASK_CATEGORIES`). Нужно было дать
  пользователю создавать свои категории, но полная нормализация
  (`Task.category` → `Category.id`) означала бы миграцию с риском для
  реальных данных задач ради фичи, которая по сути — просто "список
  того, что предлагать в выпадающем списке". Решение по той же логике,
  что раньше применили к `Goal.milestones` (JSON вместо таблицы) —
  не усложнять сверх того, что реально нужно сейчас. `Task.category`
  как был простой строкой, так и остался. (2026-09-17)
- **Светлая тема — через `next-themes`, а не свой велосипед** —
  библиотека уже была установлена (её неявно использовал `sonner.tsx`
  через `useTheme()`), просто `ThemeProvider` нигде не был подключён.
  Обнаружилось, что `:root` и `.dark` в `theme.css` были побайтово
  идентичны — реальной светлой темы не существовало вообще, несмотря на
  видимость переключателя тем в коде. Тёплая светлая палитра подобрана
  заново (тот же брендовый оранжевый акцент, что и в тёмной). (2026-09-17)
- **EN/RU — свой простой словарь, а не `react-i18next`** — обсудили с
  владельцем: библиотека дала бы больше возможностей (множественное
  число, интерполяция из коробки), но ради текущего объёма текста это
  лишняя зависимость и настройка. Свой механизм — `t(key, vars)` с
  подстановкой `{var}` в строку, два плоских словаря `en`/`ru`, язык
  хранится в `localStorage` (авторизации в приложении всё ещё нет).
  Переводятся только элементы интерфейса (кнопки, лейблы, пустые
  состояния, тосты) — названия задач/целей/привычек и категорий,
  введённые самим пользователем, никогда не переводятся. (2026-09-17)
- **`Button` (`shared/ui/button.tsx`) не оборачивал `React.forwardRef`**
  — баг того же класса, что уже был у `AccordionItem` (см. заметку в
  `qa-tester.md`): Radix-компоненты вроде `PopoverTrigger asChild`
  подключают свой `ref` к дочернему элементу через `Slot`, и без
  `forwardRef` этот `ref` тихо никуда не долетает — попап просто не
  открывался, без единой ошибки в консоли, кроме предупреждения React
  про "Function components cannot be given refs". Нашли это при
  подключении тематического date-picker (который использует
  `Popover`+`Button` как триггер). Исправлено один раз в самом `Button`
  — чинит разом все места, где он использовался с `asChild` через любой
  Radix-компонент. (2026-09-17)
- **Авторизация — Better Auth, а не самопис и не сторонний сервис** —
  обсудили с владельцем варианты (свой код с нуля, сторонний сервис
  вроде Clerk/Auth0, готовая self-hosted библиотека) и выбрали
  библиотеку поверх своей же PostgreSQL: не пишем security-критичный
  код (хэширование паролей, сессии) руками — слишком легко незаметно
  ошибиться, но и данные пользователей не отдаём наружу — тот же
  принцип, что выбор своего VPS вместо управляемых платформ. Вход по
  паролю, не по ссылке на почту (magic link обсуждали — меньше кода,
  но владелец предпочёл скорость входа привычным флоу). Email
  обязательно подтверждается перед первым входом. (2026-09-18)
- **Модели Better Auth (`User`/`Session`/`Account`/`Verification`)
  написаны в `schema.prisma` руками, не через `npx auth generate`** —
  у CLI-генератора библиотеки была история перезаписи существующих
  моделей в схеме (баг репортился, не всегда воспроизводился на разных
  версиях) — решили не рисковать четырьмя уже существующими моделями
  (`Task`/`Goal`/`Habit`/`Category`) ради удобства генерации. Саму
  миграцию тоже собрали не через `prisma migrate dev` (требует
  интерактивный терминал, недоступный в этой среде), а через
  `prisma migrate diff` (сравнение текущей базы со схемой) +
  `prisma migrate deploy` — тот же путь, что и продовый деплой уже
  использует. (2026-09-18)
- **Категории теперь привязаны к пользователю, `@@unique([userId, name])`
  вместо глобального `@unique` на `name`** — иначе после первого же
  пользователя никто другой не смог бы завести категорию "Work". Сид 4
  стандартных категорий (Work/Personal/Health/Learning) стал "ленивым"
  — создаётся при первом запросе списка категорий, если у пользователя
  их ещё нет, а не одним разом на всё приложение, как раньше. (2026-09-18)
- **Пароль пользователя хранится в таблице `account`
  (`providerId: "credential"`), не в `user`** — так устроена схема
  Better Auth; если когда-нибудь понадобится смотреть/чинить эти данные
  напрямую через SQL, искать не в `user.password` (такого поля нет
  вообще), а в `account.password` у строки с `providerId = 'credential'`.
  (2026-09-18)
- **Баг: клиентская навигация `navigate("/today")` после успешного
  входа оставляла на `/login`, хотя сессия была валидной** (прямой
  `fetch('/api/auth/get-session')` подтверждал куку/сессию рабочими) —
  `RootLayout`, монтируясь заново через SPA-навигацию, гонится с
  внутренней инвалидацией сессии клиентской библиотеки. Починили через
  `window.location.href = "/today"` (настоящая перезагрузка страницы)
  именно в этом направлении (логин → защищённая часть) — обратное
  направление (logout) не страдает, потому что там `RootLayout` уже
  смонтирован и просто реагирует на смену сессии как обычно. (2026-09-18)
- **Перенос продовых данных на владельца-пользователя — решили не
  делать вообще, просто удалили старьё** — перед выкаткой авторизации
  проверили, что реально лежит на проде: 0 задач, 0 целей, 1 привычка
  без важной истории, 4 дефолтные категории. Вся тяжёлая процедура из
  плана (репетиция на копии, построчная сверка) была рассчитана на
  случай реальных данных — раз их по сути не было, владелец сам
  предложил не переносить, а зарегистрировать чистый аккаунт и удалить
  старые "ничейные" записи. `userId` сделали `NOT NULL` сразу после
  удаления — без реальных данных это стало тривиальной миграцией, а не
  риском. Мораль: прежде чем планировать сложную миграцию — сначала
  проверь, сколько там вообще реальных данных. (2026-09-19)
- **Реальный инцидент: деплой без предварительной настройки сервера
  уронил прод на ~2 часа** — запушили код с новыми обязательными
  переменными окружения (`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`),
  обновили про них документацию (`docs/DEPLOYMENT.md`), но забыли
  реально прописать их в `/var/www/tasktracker/server/.env` на самом
  VPS **до** пуша. Контейнер приложения падал в бесконечном рестарте
  (`BetterAuthError: You are using the default secret`), база данных не
  пострадала. Починили за минуты, как только заметили (SSH → дописать
  переменные → `docker compose up -d app`). Правило на будущее: если
  новая фича требует новых переменных окружения на проде — сначала
  прописать их руками на сервере, потом уже пушить код, который их
  ожидает, а не наоборот. (2026-09-19)
- **Как отличить "первый вход после регистрации" от обычного повторного
  входа — без нового поля в БД** — Better Auth принимает `callbackURL`
  в `authClient.signUp.email(...)`, зашивает его в ссылку письма
  подтверждения и сам делает HTTP-редирект браузера туда после успешной
  верификации (подтверждено чтением исходников
  `node_modules/better-auth/dist/api/routes/email-verification.mjs`:
  `if (ctx.query.callbackURL) throw ctx.redirect(...)`). Передали
  `callbackURL: "/login?verified=1"` — `LoginPage` читает этот параметр
  и при успешном входе ведёт на `/today?firstLogin=1` вместо обычного
  `/today?welcomeBack=1`. Никаких изменений в схеме или доп. запросов к
  БД не потребовалось — только использование того, что библиотека уже
  умеет. (2026-09-19)
- **Виджет обратной связи — временная фича на период беты, с флагом на
  удаление, а не полноценная инфраструктура фиче-флагов** — владелец
  скоро даст приложение реальным пользователям на обкатку и хочет
  получать баги/пожелания, но фича не нужна навсегда. Вместо системы
  фиче-флагов — одна константа `FEEDBACK_BETA_ENABLED` в
  `src/widgets/feedback/ui/FeedbackBanner.tsx`, которую легко выставить
  в `false` или вовсе удалить компонент, когда обкатка закончится.
  Получатель писем (`FEEDBACK_EMAIL_TO`) пока не задан — почта для
  приёма отзывов ещё не создана — поэтому отзывы копятся в таблице
  `Feedback` и логах сервера, email уйдёт сам, как только адрес
  появится, без изменений кода. (2026-09-19)
- **Баг "changed elsewhere" после reorder — batch-эндпоинт `PATCH
  /reorder` тоже бампает `updatedAt`, а клиент об этом не узнавал** —
  первое предположение по аналогии с уже известным багом
  `Goal.milestones` оказалось неверным (тот баг — про единую JSON-
  колонку и `update()`; `Habit`/`Task`/`Category` реордерятся через
  отдельный batch-роут, который в принципе не проверяет
  `expectedUpdatedAt`). Настоящая причина — в другом месте: Prisma's
  `@updatedAt` обновляет `updatedAt` у каждой строки при **любой**
  записи, включая `updateMany({data: {order}})` в reorder-эндпоинте, а
  `PATCH /reorder` отвечал голым `204` без данных — фронтенд продолжал
  считать актуальным старый (уже неверный) `updatedAt` для всех
  переставленных записей. Следующий точечный `PATCH /:id` (например,
  отметить привычку выполненной сразу после перетаскивания — самый
  частый сценарий) отправлял этот протухший `expectedUpdatedAt` и
  получал ложный `409`. Фикс — `PATCH /reorder` теперь возвращает `{id,
  updatedAt}[]`, `usePersistedCollection.reorder()` подмешивает свежие
  значения в локальный стейт сразу после успешного reorder. Урок: если
  один и тот же паттерн бага всплывает у нескольких похожих сущностей —
  не полагаться на аналогию с уже объяснённым случаем, перепроверять
  код каждой сущности отдельно, точная причина может отличаться.
  (2026-09-19)
- **Реордер списка `Goal` изначально забыли починить вместе с
  `Habit`/`Task`/`Category`** — тот же баг из пункта выше (`PATCH
  /reorder` не возвращал свежий `updatedAt`) существовал и у списка
  целей, но правку туда не перенесли сразу, т.к. на тот момент
  внимание было на `Goal.milestones` (другой, уже известный конфликт).
  Нашлось не тестированием, а при плановой синхронизации
  `docs/requirements/` — `docs-writer` агент, актуализируя документацию
  по бэкенду, заметил несоответствие "по коду это ещё 204, а у соседних
  трёх сущностей уже 200 с телом" и явно об этом сообщил, не просто
  промолчал. Урок: точечная синхронизация документации после фикса —
  не формальность, иногда буквально ловит забытое место, которое обычный
  ручной тест мог не задеть. (2026-09-19)
- **Найден и закрыт общий корень давнего бага "changed elsewhere" у
  подцелей — не точечный фикс, а общая защита в `usePersistedCollection.
  update()`** — владелец заметил ту же ошибку при перетаскивании
  подцелей внутри цели и попросил не чинить точечно, а поискать, нет ли
  такой же проблемы в других местах. Оказалось, что причина — не
  особенность конкретно `Goal.milestones` (как предполагалось в
  `docs/BACKLOG.md` ещё с 2026-09-17), а системная дыра в самой функции
  `update()`: она не защищена от двух почти одновременных вызовов для
  одной и той же записи — второй вызов читает `expectedUpdatedAt` из
  React-стейта, который ещё не знает о результате первого. Перетаскивание
  подцелей вызывает `update()` на каждый шаг наведения (`onHoverMove`),
  поэтому гонка воспроизводится почти всегда, а не изредка. Фикс —
  очередь вызовов на уровне `id`: `usePersistedCollection.ts` хранит
  `Map<id, Promise>` последних вызовов и каждый следующий вызов для того
  же `id` дожидается предыдущего и берёт `updatedAt` из его
  подтверждённого сервером результата, а не из потенциально устаревшего
  стейта. Это чинит не только подцели, а вообще любые два быстрых подряд
  изменения одной записи (`create`/`remove`/`reorder` этой защиты не
  требуют — они либо не проверяют `expectedUpdatedAt` вообще, либо
  всегда работают с новым id). Написан регресс-тест с управляемой
  задержкой в фейковом репозитории, который сначала подтвердил баг на
  старом коде (падал с той же `ConflictError`, что видел владелец), а
  затем прошёл на исправленном. Урок: если баг с самого начала выглядит
  как "иногда бывает у одной конкретной сущности" — стоит подозревать
  общий код, которым эта сущность пользуется, а не только её саму;
  когда владелец сам подсказывает "поищи, не смежное ли это" — это
  правильный вопрос, и он тут привёл к более простому и общему фиксу,
  чем если бы чинили только `Goal.milestones` точечно. (2026-09-20)
- **Каскадное удаление категории с задачами — синхронизация через
  `refresh()`, а не через явную передачу id между сущностями** —
  `Task.category` — обычная строка, не внешний ключ (сознательный
  выбор с самого начала, см. выше), поэтому при удалении категории
  бэкенд теперь сам находит и удаляет её задачи одной транзакцией по
  совпадению имени. Но фронтенд-провайдер задач (`usePersistedCollection`)
  не мог узнать об этом сам — вариант "передать id удалённых задач
  явно между `CategoryProvider` и `TaskProvider`" усложнил бы общий
  переиспользуемый хук ради одного частного случая. Вместо этого в
  `usePersistedCollection` добавили простой универсальный `refresh()`
  (перезапросить список с сервера) — `TaskBoard.tsx` вызывает его
  сразу после успешного удаления категории. Общее решение полезнее
  точечного: пригодится для любого будущего случая "что-то изменилось
  на сервере не через привычный create/update/remove". (2026-09-20)
- **Цвета привычек и целей были одним и тем же списком, который
  просто рассинхронизировался** — `GOAL_COLORS` (5 значений) оказался
  побайтово идентичным первым 5 значениям `HABIT_COLORS` (12
  значений) — кто-то в прошлом скопировал часть списка вместо того,
  чтобы сослаться на общий. Вынесли в `src/shared/lib/colors.ts`
  (`PALETTE_COLORS`), обе формы используют один и тот же массив —
  чтобы это не повторилось в третий раз. (2026-09-20)
- **"Лениво сидим при первом обращении" не может опираться на
  `count === 0`, если пользователь может обнулить список сам** —
  сид 4 дефолтных категорий проверял "есть ли у пользователя хоть одна
  категория", а не "сидился ли он уже". Разницы не было видно, пока
  удаление категории не удаляло сами задачи — как только это
  появилось (2026-09-20), "удалить последнюю категорию" стало обычным
  осознанным действием, а не редким крайним случаем, и дефолты
  начинали "воскресать" при следующей загрузке. Фикс — явный флаг
  `User.categoriesSeeded`, а не пересчёт count: выставляется в true и
  при сидировании дефолтов, и при первом же ручном создании категории
  (на случай если POST дошёл раньше первого GET — сама возможность
  такой гонки всплыла только при переносе провайдеров сущностей ниже
  сессии, см. следующий пункт, и была закрыта через `skipDuplicates:
  true` в `createMany`, а не отдельной блокировкой запросов). Миграция,
  добавляющая колонку, сразу бэкфиллит `true` всем, у кого на тот
  момент уже есть хоть одна категория — те, у кого категорий 0 в
  момент миграции, неотличимы (новый пользователь или уже всё удалил)
  и один последний раз получат дефолты заново, это осознанная разовая
  граница фикса, не баг. Урок: булев флаг "уже произошло", а не
  производный count, — правильный инструмент для "сделать что-то
  ровно один раз за жизнь сущности", если у этого "что-то" есть
  обратимое действие (тут — удаление). (2026-09-21)
- **Провайдеры сущностей грузили данные ещё до входа — потому что
  были смонтированы выше роутера, а не выше на неудачу сессии** —
  `TaskProvider`/`GoalProvider`/`HabitProvider`/`CategoryProvider`
  жили в `AppProviders.tsx`, который оборачивает `<RouterProvider>`
  целиком; единственная проверка сессии (`RootLayout`, редирект на
  `/login`) была уже внутри роутера, то есть ниже них по дереву — сама
  проверка ничего не блокировала, т.к. провайдеры уже успели
  смонтироваться и дёрнуть `fetch` на `mount` (`usePersistedCollection`)
  независимо от того, что решит `RootLayout`. Фикс — не добавлять
  проверку сессии внутрь `usePersistedCollection` (это раздуло бы
  общий хук ради одного частного случая), а просто перенести эти 4
  провайдера физически внутрь `RootLayout`, после `if (!session)
  return <Navigate />` — тогда они в принципе не существуют в дереве,
  пока сессии нет. `ThemeProvider`/`LanguageProvider`/`DndProvider`/
  `TooltipProvider`/`Toaster` остались в `AppProviders`, т.к. нужны и
  `/login`. Урок: "тут нет проверки авторизации" и "эта проверка есть,
  но не там, где начинается работа с данными" — разные баги; второй
  не находится поиском "а где у нас auth guard", только чтением
  фактического дерева компонентов. (2026-09-21)
- **Better Auth: своё правило пароля сверх встроенного — через
  `hooks.before`, а не через конфиг** — библиотека из коробки проверяет
  только длину пароля (`minPasswordLength`/`maxPasswordLength`),
  никакого способа задать "хотя бы буква и цифра" в самом конфиге нет.
  Нашли в исходниках пакета механизм `hooks: { before: createAuthMiddleware(...) }`
  (импорт из `"better-auth/api"`, не из корня `"better-auth"`) — функция,
  которая выполняется перед любым запросом к `/api/auth/*`, может
  проверить `ctx.path` и `ctx.body` и оборвать запрос своей `APIError`
  до того, как библиотека вообще начнёт что-то делать. Применили на
  всех трёх путях, где реально ставится новый пароль (`/sign-up/email`,
  `/change-password`, `/reset-password`) — забыть один из них означало
  бы дыру, через которую правило можно обойти. Проверено не только
  юнит-тестами существующих сценариев (они не ловят это — тестовые
  пароли и так с буквой и цифрой), а отдельным ручным скриптом,
  который явно послал слабые пароли на все три пути и убедился в `400`
  с понятным кодом. Урок: если у сторонней библиотеки есть "часть
  правил", а не нужна ещё пара своих — искать её hook-механизм
  до того, как писать это руками отдельным Express-мидлваром перед её
  роутами (что было бы сложнее и легко разошлось бы с самой библиотекой
  по порядку проверок). (2026-09-21)
- **`resize_window` (MCP-инструмент Chrome) не работает в этом
  окружении** — при попытке проверить, что кнопка-шестерёнка настроек
  не перекрывается с нижней мобильной навигацией на узком экране,
  инструмент стабильно рапортует успех, но реальное окно браузера не
  меняет размер (воспроизведено дважды, разными размерами, тем же
  результатом столкнулся и `qa-tester` агент в этой же итерации
  независимо). Не баг приложения — ограничение самого инструмента в
  этой среде. Проверка мобильной ширины для этого конкретного случая
  сделана через чтение кода (`fixed top-4 right-4` у шестерёнки против
  `fixed bottom-0` у мобильной навигации — разные края экрана,
  пересечься не могут ни при какой разумной ширине), а не визуально.
  На будущее: если снова понадобится реальный скриншот на узкой
  ширине — не полагаться на `resize_window`, сразу предупредить
  владельца и попросить ручную проверку. (2026-09-21)
- **Дедуп-ключ в `localStorage` без учёта пользователя — тихая дыра
  на общем браузере** — тосты-напоминания о сроке (`src/widgets/
  due-date-reminders/`) изначально дедуплицировались только по дате
  (`mytracker-reminders-shown-<дата>`). На первый взгляд рабочее
  решение, но если на одном браузере в один день заходят два разных
  аккаунта — тост первого выставляет флаг "уже показано сегодня", и
  второй аккаунт свой тост просто не увидит, без единой ошибки где
  либо. Нашёл `qa-tester` агент при проверке (не специально искал,
  просто описал как "design note"), в тот же день исправлено —
  ключ стал `mytracker-reminders-shown-<userId>-<дата>`. Урок: любой
  ключ в `localStorage`/`sessionStorage`, который отвечает за "уже
  показывали/уже делали" — по умолчанию проверять, не нужна ли
  привязка к пользователю, а не только к времени, если приложение
  поддерживает несколько аккаунтов на одном устройстве. (2026-09-21)
- **Бесконечные `500` при перетаскивании — не баг конкретно задач, а
  дыра в общей drag-and-drop инфраструктуре, замаскированная под
  серверную ошибку** — владелец прислал скриншот: перетащил задачу,
  консоль тут же залилась повторяющимися `Failed to reorder /api/tasks:
  500`, ушло только после перезагрузки страницы. Первое, неверное
  предположение — искать баг в `tasks.ts`. Настоящая причина — связка
  двух вещей в общем коде, которым пользуются все сущности с
  drag-and-drop: `react-dnd`'s `hover` стреляет на каждый тик движения
  мыши, не только когда цель реально сменилась (`useDragReorder.ts`
  раньше проверял только "не наводить на себя"); и
  `usePersistedCollection.reorder()`, в отличие от уже защищённого
  `update()` (см. запись про очередь `pendingUpdates`, 2026-09-20), не
  дожидался предыдущий вызов — каждый `hover`-тик сразу улетал отдельным
  запросом. Один жест перетаскивания = десятки параллельных
  `PATCH .../reorder`, каждый — своя транзакция Postgres над теми же
  строками → конкурентные транзакции ловят deadlock → Prisma кидает
  необработанную ошибку → голый `500`, и так на каждый такой запрос,
  что и выглядело как "бесконечные ошибки". Фикс — на всех трёх
  уровнях сразу: `hover` теперь не стреляет повторно для уже
  обработанного перемещения; `reorder()` получил такую же очередь
  вызовов, как `update()`; на бэкене (для всех четырёх сущностей —
  задачи/цели/привычки/категории, не только задач) — транзакция теперь
  сортирует обновления по `id` перед отправкой, чтобы конкурентные
  транзакции всегда брали блокировки строк в одном порядке
  (стандартный приём против deadlock'ов, подстраховка даже если
  фронтенд-фикс где-то не сработает). Урок: голый `500` от Prisma —
  не всегда "баг в этом конкретном роуте"; если рядом есть
  drag-and-drop с частым `hover`, для начала стоит посчитать, сколько
  запросов реально улетает за одно перетаскивание, прежде чем
  разбирать сам SQL. (2026-09-21)
- **Инструмент `read_network_requests` (Chrome MCP) стабильно врёт про
  статус `DELETE`-запросов в этой среде** — воспроизведено дважды
  независимо разными `qa-tester` агентами в один день: инструмент
  показывает `503` на каждый `DELETE`, хотя реальный лог бэкенд-процесса
  и фактическое состояние базы после перезагрузки страницы подтверждают
  настоящий `204`. Не баг приложения — искажение самого инструмента
  сетевой инспекции конкретно для `DELETE`. На будущее: если
  `read_network_requests` показывает `503`/ошибку на `DELETE`, сверяться
  с логом сервера (`docker compose logs`/`backend-dev.log`) или с
  реальным состоянием после перезагрузки, а не доверять инструменту
  вслепую — та же логика уже применялась к `resize_window`
  (см. запись выше). (2026-09-21)
- **Виджет не может напрямую импортировать другой виджет — Steiger
  ловит это как `fsd/forbidden-imports`, error, не warning** — всплыло
  при первой попытке собрать карточку-переключатель "Прогресс целей ↔
  Напоминания" (нужно было объединить два уже существующих виджета,
  `goal-progress-summary` и новый `reminder-summary`, в один компонент
  со свапом). Первая версия жила в `src/widgets/goal-reminder-swap/` —
  `npm run fsd-lint` сразу дал ошибку (не предупреждение, как обычно
  бывает с "insignificant-slice"). Правило FSD: слои строго
  однонаправленные, виджет может опираться на entities/features/shared,
  но не на другой виджет тем же уровнем — комбинировать несколько
  виджетов имеет право только то, что стоит выше них, т.е. страница.
  Фикс — просто перенести компонент из `widgets/` в
  `pages/today/ui/components/` (страница уже имеет право импортировать
  сколько угодно виджетов и сама их компоновать) — без изменений в
  самой логике. На будущее: если новая фича — это "взять два уже
  готовых виджета и как-то их объединить/переключать", такой
  объединяющий компонент по умолчанию должен жить на уровне страницы,
  а не заводить для него отдельный виджет. (2026-09-22)
- **Ленивый `useState`-инициализатор, читающий данные из асинхронно
  загружаемого контекста, видит их пустыми — даже если на сервере они
  реально есть** — карточка-стопка на Today должна была сама открываться
  на "Напоминаниях", если у пользователя есть незавершённые напоминания
  (`useState(() => summarizeReminders(reminders).total > 0)` в
  `GoalReminderSwapCard.tsx`). На практике карточка ВСЕГДА открывалась на
  "Прогресс целей", даже с реальными напоминаниями в базе — нашёл
  `qa-tester` агент, воспроизводилось стабильно 100%. Причина: ленивая
  функция в `useState` выполняется один раз, на самом первом рендере —
  а `reminders` в этот момент всегда `[]`, потому что
  `usePersistedCollection` ещё не успел получить ответ от `GET
  /api/reminders` (запрос асинхронный). К моменту, когда данные реально
  приходят, инициализатор уже отработал и никогда не перезапускается.
  Фикс — не читать проп в момент создания стейта, а дождаться сигнала,
  что данные реально загружены: `usePersistedCollection` уже отдаёт
  `isLoaded`, прокинул его через `TodayProvider`/`todayContext` как
  `remindersLoaded`, и в `GoalReminderSwapCard` решение теперь
  принимается в `useEffect`, который ждёт `remindersLoaded === true` и
  срабатывает ровно один раз (`useRef`-флаг) — как и раньше, не
  передёргивает карточку туда-сюда при последующих изменениях списка.
  Урок: если "начальное состояние UI должно зависеть от данных,
  которые могут быть ещё не загружены" — использовать `isLoaded`-флаг
  сущности и `useEffect`, а не ленивый `useState`-инициализатор,
  который видит только то, что есть в пропсах в момент самого первого
  рендера. (2026-09-22)
- **`PopoverTrigger` без `forwardRef` — пятый случай того же класса
  багов**, самый тихий из всех: клик по попапу продолжал работать
  месяцами (в `DatePicker`), сломалось только когда `Tooltip` впервые
  обернул `PopoverTrigger` через `asChild` (`ReminderBell.tsx`,
  наведение на глобальный колокольчик не показывало тултип). Подробности
  и вся хронология всех пяти случаев — `docs/ARCHITECTURE.md`, раздел
  "Вендорные компоненты без `forwardRef`". (2026-09-22)
- **"Бессрочная задача видна на Today только в день создания" — от
  первого фикса (2026-09-21) до жалобы пользователя (2026-09-23) не
  дошло, что "не показывать вечно" и "убирать уже на следующий день,
  даже если не выполнена" — это разные требования** — исходный баг
  был "показывается вечно, даже выполненная" (`dueDate == null` без
  привязки к дате вообще), пофиксили строго по дню создания — и заодно
  случайно убрали то, что пользователю было реально нужно: возможность
  видеть НЕвыполненную задачу без даты на Today и на следующий день, и
  через день, пока не дойдут руки её сделать. Правило, которое
  оказалось верным (`selectTodayTasks.ts`): для бессрочной задачи
  важна не дата СОЗДАНИЯ, а статус ВЫПОЛНЕНИЯ — невыполненная видна
  всегда, выполненная — только в день `completedAt`, дальше пропадает.
  Урок: когда фикс формулируется как "X пропадает по времени" — стоит
  сразу уточнять, должно ли это зависеть от завершённости задачи, а не
  только от даты, особенно для сущностей без явного срока. Закрыли
  тестом (`selectTodayTasks.test.ts`) — второй раз переписывать эту
  функцию по недопониманию не хочется. (2026-09-23)
- **Перетаскивание привычки между блоками (фича группировки привычек)
  ловило кажущийся один и тот же баг ЧЕТЫРЕ раза подряд — каждый
  qa-tester-прогон чинил один слой и тут же натыкался на следующий,
  под ним.** Симптом каждый раз одинаковый: тост "Эта запись была
  изменена в другом месте" + `409` на `PATCH /api/habits/:id`. Причины
  оказались РАЗНЫЕ, послойно:
  1. `useDropTarget` — вложенные drop-таргеты одного типа (строка
     привычки внутри заголовка блока, оба принимают `"habit-move"`)
     стреляли `onDrop` ДВАЖДЫ на один drop (react-dnd вызывает `drop`
     снизу вверх по всем подходящим предкам) — два конкурентных
     `update()` на одну запись. Фикс — `monitor.didDrop()` в самом
     хуке (`src/shared/lib/dnd/useDropTarget.ts`), чтобы внешний
     drop-таргет пропускал уже обработанный drop. Это общий хук —
     фикс закрывает класс бага для любых будущих вложенных drop-зон
     одного типа, не только для привычек.
  2. `moveHabitToGroup` — запускал `update(groupId)` и `reorder(...)`
     параллельно (`reorder` не ждал `update`), это два НЕЗАВИСИМЫХ
     друг от друга запроса к одной записи. Фикс — `await update(...)`
     перед вызовом `reorder(...)`.
  3. `usePersistedCollection.reorder()` при вызове с ПОДМНОЖЕСТВОМ
     коллекции (а не всей целиком) — `setItems(nextItems)` тогда
     стирал из локального стейта все элементы, которых не было в этом
     подмножестве (привычки из других блоков временно "пропадали" из
     UI и из счётчика, пока не перезагрузишь страницу). До этой фичи
     `reorder()` ВСЕГДА вызывался с полным списком (задачи, цели,
     категории, привычки на Today) — группировка привычек первой
     стала передавать туда подмножество (`reorderHabitsInGroup`,
     `moveHabitToGroup`), и это допущение впервые сломалось. Фикс —
     `reorder()` теперь сохраняет элементы вне переданного
     подмножества, а не заменяет стейт целиком.
  4. Самый глубокий — `pendingUpdates` (очередь, которая чинит гонку
     ДВУХ подряд идущих `update()` одного id, см. запись про эту же
     очередь от 2026-09-20/21) ничего не знала про `reorder()`,
     который тоже бампает `updatedAt` на сервере (это `@updatedAt`
     Prisma-колонка, бампается при любой записи, включая reorder).
     Поэтому ВТОРОЕ перемещение одной и той же привычки подряд (после
     того как первое уже прошло `update()`+`reorder()`) слало
     `expectedUpdatedAt` из ответа ПЕРВОГО `update()`, игнорируя, что
     `reorder()` после него ещё раз сдвинул `updatedAt` на сервере —
     сервер был прав, отклоняя запрос, но с точки зрения пользователя
     это ложный, необъяснимый провал второго перетаскивания (и в
     одном воспроизведении реальное перемещение в БД тихо не
     применилось, хотя UI показывал другое). Фикс — `reorder()`
     теперь, получив от сервера подтверждённый `updatedAt` для
     затронутых id, тут же "прописывает" его в `pendingUpdates`, чтобы
     следующий `update()` того же id знал актуальное состояние, а не
     последнее, что знал `update()`-only чейн.
  Урок: когда `update()` и `reorder()` (или вообще любые два разных
  метода `usePersistedCollection`) трогают одну и ту же запись в
  быстрой последовательности — мало починить видимый симптом на одном
  уровне и погнать код в продакшн; стоит сразу продумать ВСЕ пары
  операций, которые могут пересечься на одном id, а не только ту,
  что попалась первой. Все четыре слоя всплыли только благодаря
  повторным прогонам `qa-tester` с явным требованием "повтори
  перетаскивание N раз подряд, без перезагрузки между попытками" —
  однократный тест каждый раз показывал "исправлено". (2026-09-23)
- **`justify-center` на `AccordionTrigger` не центрировал контент — потому
  что триггер вообще не растягивался на всю ширину карточки**, когда стоял
  в одной flex-строке с соседним div (кнопки редактировать/удалить) —
  `TodayHabitGroupCard.tsx`, новая "пилюля"-шапка блока на Today.
  Причина: flex-элемент без явного `flex-grow` (а `AccordionPrimitive
  .Header`, оборачивающий сам `AccordionTrigger`, именно такой) размером
  "по контенту" (`fit-content`), а не растягивается на свободное место в
  строке — растягивание происходит по умолчанию только когда элемент
  ЕДИНСТВЕННЫЙ ребёнок родителя, который сам НЕ flex (тогда работает
  обычное блочное поведение "ширина 100% родителя"). На `/habits` та же
  структура (`AccordionTrigger` + соседний div с кнопками в одной flex-
  строке) работает "незаметно", потому что там контент триггера и так
  левовыровненный — баг был архитектурно тем же самым, просто не виден
  визуально. Фикс — убрать соседство в строке: обёртка становится
  `relative` (не `flex`), кнопки редактирования/удаления — `absolute
  right-4 top-4` (вне потока), тогда `AccordionTrigger` остаётся
  единственным ребёнком и наконец растягивается на всю ширину, и
  `justify-center` получает реальное свободное место, чтобы centered.
  Второй слой той же находки: после этого фикса шеврон всё ещё улетал в
  правый край, а не сидел рядом с "пилюлей" — потому что на самой
  "пилюле" остался класс `mx-auto` (нужный для СТАРОЙ, нерабочей версии
  вёрстки) — auto-margins на flex-элементе перетягивают на себя ВСЁ
  свободное место главной оси в приоритете перед `justify-content`
  родителя, расталкивая соседний элемент (шеврон) к краю контейнера.
  Урок: если добавляешь `justify-center`/`justify-between` на flex-
  родителя — сначала проверить, что у родителя ЕСТЬ реальное свободное
  место для распределения (не зажат ли он в строке с "прожорливым"
  соседом), и не осталось ли на самих детях `mx-auto`/других auto-margin
  классов от предыдущей версии вёрстки — они тихо выигрывают у
  `justify-content` без единой ошибки в консоли. Нашёл и исправил через
  прямую проверку `getBoundingClientRect()`/`getComputedStyle()` в
  консоли браузера — быстрее, чем гадать по скриншотам. (2026-09-24)
- **Продолжение той же "пилюли" — отрицательный `margin-top`, которым
  затащил её на верхнюю границу карточки, съедал место у СОСЕДНЕЙ
  карточки, а не только у своей.** По просьбе пользователя убрал рамку у
  свёрнутого блока и оставил только пилюлю "наезжающей" на рамку сверху,
  когда блок раскрыт (`TodayHabitGroupCard.tsx`). Пока хотя бы один блок
  на экране был раскрыт — всё смотрелось нормально; как только ОБА блока
  сворачивались — пилюли соседних карточек наезжали друг на друга и на
  кнопки редактирования/удаления (пользователь прислал скриншот).
  Причина: `-mt-3.5` на `AccordionTrigger` — это отрицательный margin, а
  не просто визуальный сдвиг, и у первого дочернего элемента блока такой
  margin может "схлопнуться" с отступом родителя, реально уменьшая
  высоту, которую карточка резервирует в потоке разметки — у свёрнутой
  карточки эта высота и так минимальна (только сама пилюля), поэтому
  `space-y-3` (12px) между карточками перестало хватать. Фикс — два
  слоя: (1) заменил `-mt-3.5` (margin) на `relative` + `top: -3.5`
  (визуальный сдвиг, не трогает раскладку — карточка резервирует место
  как обычно, пиксели просто рисуются выше), обёртку вернул на `pt-3.5`,
  чтобы зарезервированное место совпадало со сдвигом; (2) увеличил
  `space-y-3` → `space-y-5` в `HabitTrackerGrid.tsx` — сдвиг в 14px
  всё равно "выходит" за пределы box'а визуально (сама суть приёма), и
  между карточками должен быть больше зазор, чем этот сдвиг, а не в
  притык к нему. Урок: если элемент должен визуально вылезать за
  границу своего блока (наезжать на рамку, чипсы поверх карточки и
  т.п.) — двигать его через `position: relative` + `top/left`, а не
  через отрицательный `margin`; margin меняет реально резервируемое
  место в раскладке и может "утекать" в соседние элементы неожиданным
  образом, особенно в сжатых/свёрнутых состояниях, которые не видны,
  пока не проверишь именно их. (2026-09-24)
- **Компонент, который просто объединяет несколько уже готовых виджетов,
  ломает FSD, если завести под него отдельный виджет.** При постройке
  календаря в стиле TickTick (5 видов: Agenda/День/Неделя/Месяц/Год)
  первая версия `CalendarMonthView` жила в `src/widgets/calendar-month/`
  и импортировала `CalendarGrid` (виджет `calendar-grid`) и
  `DayDetailPanel` (виджет `day-detail-panel`) — `npm run fsd-lint`
  (Steiger) тут же поймал это как `fsd/forbidden-imports`, ошибку, а не
  предупреждение: в этом проекте виджет-соседей импортировать друг в
  друга нельзя. Фикс — перенести файл в `src/pages/calendar/ui/
  CalendarMonthView.tsx` (уровень страницы) и превратить его из
  потребителя контекста в чисто пропсовый компонент (контекст
  `CalendarProvider` живёт в `pages/calendar`, а не в `widgets`, так что
  виджет и его прочитать не смог бы). Это ровно тот же паттерн, что уже
  был найден раньше в этой же сессии на `GoalReminderSwapCard` на
  странице Today — но тогда он не был записан как общее правило, из-за
  чего пришлось наступить на те же грабли повторно при планировании
  следующих 4 видов календаря (Agenda/День/Неделя тоже сразу
  спроектированы как `pages/calendar/ui/*`, не как виджеты).
  Урок: если новый компонент существует только для того, чтобы
  скомпоновать 2+ уже существующих виджета вместе — он по умолчанию
  живёт на уровне **страницы**, а не заводится под него ещё один виджет
  "на всякий случай/по аналогии". Настоящим виджетом (в `src/widgets/`)
  в этом проекте остаётся только то, что само НИ ОТ ОДНОГО
  виджета-соседа не зависит — так, `calendar-timeline` (общая часовая
  шкала для Дня/Недели) и `calendar-year` (self-contained, использует
  только `shared/lib/calendarGrid.ts`) — легитимные виджеты, а
  `calendar-{month,agenda,day,week}` — компоненты страницы. Стоит
  формулировать это правило явно себе в план ДО начала реализации
  следующей похожей фичи, а не полагаться, что прецедент вспомнится сам.
  (2026-09-24)
- **Расширение сигнатуры колбэка в общем хуке — самый простой способ
  добавить данные, не трогая существующих вызывающих.** Часовой шкале
  (`calendar-timeline`) нужен был Y-пиксель сброса перетаскивания, чтобы
  превратить его в конкретное время — а `useDropTarget`
  (`src/shared/lib/dnd/useDropTarget.ts`) отдавал колбэку `onDrop`
  только `draggedId`, без позиции. Вместо отдельного нового хука
  специально под этот один сценарий — добавил `onDrop`'у второй,
  необязательный по факту использования аргумент
  (`monitor.getClientOffset()`), который все существующие вызовы (сетка
  месяца, привычки и т.п., принимающие колбэк с одним параметром) просто
  игнорируют — в JS/TS лишний переданный аргумент у функции с меньшим
  числом объявленных параметров не ошибка. Единственное, что пришлось
  поправить — mock-тест `useDropTarget.behavior.test.tsx`, где мок
  `monitor` не имел метода `getClientOffset` и падал с `TypeError`.
  Урок: перед тем как заводить новый хук/утилиту под чуть более богатый
  частный случай существующего паттерна — проверить, нельзя ли просто
  расширить сигнатуру существующего колбэка так, чтобы старые вызовы
  остались рабочими без изменений; это меньше кода и не плодит почти
  идентичные абстракции рядом друг с другом. (2026-09-24)
- **`e.stopPropagation()` на `pointerdown` не гасит последующий `click`.**
  При добавлении ручки растягивания задачи на часовой сетке
  (`HourGrid.tsx`, `useResizeDrag`) обнаружилось: после растягивания
  задачи за нижний край (ручка сама вызывала `stopPropagation()` +
  `preventDefault()` в своём `onPointerDown`, чтобы не запускать
  whole-block drag и клик-редактирование родительского блока) — попап
  редактирования всё равно открывался сразу после отпускания мыши.
  Причина: `stopPropagation()` на событии `pointerdown` останавливает
  только САМО это событие от всплытия — браузер после `mouseup`
  независимо синтезирует и диспатчит отдельное событие `click` с
  собственным путём всплытия, которое `stopPropagation()` на другом типе
  события (`pointerdown`) никак не перехватывает. Событие `click`
  беспрепятственно всплывало от ручки до родительского блока и
  срабатывал его `onClick`. Фикс — отдельный `onClick={e =>
  e.stopPropagation()}` именно на самой ручке, независимо от
  `onPointerDown`'а. Урок: если дочерний интерактивный элемent должен
  "проглотить" жест, чтобы он не долетел до родительского обработчика
  клика — гасить нужно КАЖДЫЙ тип события отдельно (`pointerdown` для
  MOVE-логики, `click` отдельно для клика), а не полагаться, что
  `stopPropagation()` одного события автоматически подавляет все
  синтетические события, которые браузер генерирует из того же
  физического жеста. Поймано и исправлено через живое QA в браузере
  (`mcp__claude-in-chrome__*`) — растягивание визуально применялось
  верно, но затем сразу же открывался попап с уже применёнными новыми
  значениями, что и выдало причину. (2026-09-24)
- **После `npx prisma generate` уже запущенный dev-сервер (`tsx watch`)
  не подхватывает новый клиент автоматически.** При добавлении
  `Task.endTime` миграция и `prisma generate` были выполнены штатно, но
  `POST /api/tasks` с `endTime` в теле всё равно падал с 500
  (`PrismaClientKnownRequestError: The column endTime does not exist in
  the current database` — притом что колонка точно была в БД). Причина:
  `tsx watch` перезапускает процесс при изменении файлов в `src/`, но не
  отслеживает `node_modules/@prisma/client` — уже запущенный Node-процесс
  держал в памяти СТАРЫЙ сгенерированный клиент с момента старта, даже
  после того как `prisma generate` переписал файлы на диске. Фикс —
  `touch server/src/index.ts` (любое изменение отслеживаемого файла),
  чтобы `tsx watch` перезапустил процесс и заново заимпортил свежий
  `@prisma/client`. Второй, отдельный от этого, гочи — тесты бэкенда
  используют СВОЮ БД (`tasktracker_test` из `.env.test`, не
  `tasktracker` из `.env`) — миграцию нужно применять в обе базы
  (`npx prisma migrate deploy` дважды, с разным `DATABASE_URL`), иначе
  тесты падают той же ошибкой независимо от dev-сервера. Урок: после
  любого изменения `schema.prisma` в этом проекте — не только применить
  миграцию и перегенерировать клиент, но и (1) перезапустить уже
  запущенный dev-сервер вручную, (2) применить миграцию к тестовой БД
  тоже, а не только к основной dev-базе. (2026-09-24)
