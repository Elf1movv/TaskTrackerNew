
  # Task and Goal Tracker

  This is a code bundle for Task and Goal Tracker. The original project is available at https://www.figma.com/design/i7GY9S5IW00gqkPNs3TVol/Task-and-Goal-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  